请升级「黄帝养成系统」文渊阁/CET-6 英语模块，使其支持导入本词库包。

导入包文件：
- cet6_vocab_full_5523_huangdi.json
- 或分块文件 cet6_vocab_part_01.json ~ cet6_vocab_part_06.json

必须支持字段：
id, word, phonetic, pos, meaning, rawMeaning, phrase, sentence, familiarity, wrongCount, correctCount, note, tags, sourceOrder

功能要求：
1. 在文渊阁增加「导入词库」入口，支持上传 JSON 文件。
2. 支持完整 JSON 数组导入，也支持分块导入。
3. 导入后存入 localStorage。
4. 导入时不要覆盖用户已有错词 note；如果同词已存在，应保留更高 wrongCount 和已有 note。
5. 今日词库可以从完整词库中选择/抽取单词。
6. 记忆模式：只显示英文单词，不显示中文释义，按钮为「记得 / 模糊 / 忘记」。
7. 点击「模糊」或「忘记」时，弹出备注输入，保存到错词本。
8. 错词本展示：英文、中文意思、词性、音标、备注。
9. 拼写试炼：显示中文释义，用户输入英文；答对显示正确并自动跳转下一题；答错弹窗提示错误并加入错词本，必须拼写正确后才能进入下一题。
10. 阅读秘境：题目答案不要提前显示；用选项按钮答题，提交后再显示正确/错误、正确答案、中文解析。
11. 阅读提交后，文章内单词可点击；弹窗显示该词中文意思和词性，并提供「加入错词本 / 不加入」按钮。

注意：
- 不要更改现有页面路由。
- 不要删除原有任务、番茄钟、复盘、属性、成就功能。
- 只升级英语模块的数据导入与训练交互。
